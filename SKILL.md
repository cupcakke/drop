Skill: Full-Repository GPU-Resident Fused Training Upgrade
1. Read every tracked file from root through final byte including Zig, Futhark, C, Python, build files, duplicates.
2. Map all imports and call sites for accelerator interfaces, Futhark sources, bindings, coordinator, NCCL, CUDA, tokenizer, embedding, RSF, OFTB, checkpoint, inference, tests.
3. Identify canonical Futhark CUDA source from build configuration; verify ABI matches generated output; remove stale duplicates.
4. Replace per-layer training state with packed authoritative GPU state: weights_s, weights_t, velocity_s, velocity_t as flat f16 arrays with exact shape [num_layers][half_dim][half_dim+1].
5. Compute exact model parameters: 2 * half_dim * (half_dim + 1) * num_layers. For 3B use half_dim=11200, num_layers=12 => 3,015,379,200 params.
6. Initialize packed state directly without GPU-to-CPU readback; implement safe packed-state init, scale, raw device access, checkpoint extraction/replacement, inference access, destruction.
7. Implement one canonical fused Futhark CUDA entry receiving inputs, targets, packed weights, packed velocities, sequence lengths/masking, learning rate, momentum, reconstruction alpha, forward scale, RSF clip limits, and all other mathematical values.
8. The fused entry must execute complete multi-layer RSF forward, OFTB forward after each layer, masked final loss, masked initial output gradient, reverse traversal through all layers, OFTB backward, reversible RSF input reconstruction, RSF gradients for both parameter matrices, optimizer velocity updates, RSF parameter updates, masked reconstruction loss, reconstruction-gradient composition, final input-embedding gradient, updated packed weights, updated packed velocities, scalar training loss, scalar reconstruction loss.
9. Preserve all RSF/OFTB mathematics, masking semantics, reconstruction semantics, finite-value handling, clipping behavior, f16/f32 conversion, optimizer behavior, distributed weighted-loss semantics.
10. Update Futhark source, Zig Futhark declarations, opaque tuple declarations, projection declarations, free declarations, tensor wrappers, shape accessors, raw device-pointer accessors, ownership handling so all match generated CUDA ABI exactly.
11. Regenerate CUDA Futhark library during validation; do not link CPU and CUDA libraries together when they expose duplicate symbols.
12. Implement deterministic vocabulary sharding by contiguous token-ID ranges from vocab_size, world_size, rank. Define exact shard metadata: global vocab size, world size, rank, shard start/end, local shard vocab size, token-owner calculation, local-row calculation, validation that every valid global token belongs to exactly one rank.
13. For world_size > 1 allocate only local source embedding rows, local optimizer state, local frozen target rows. Never allocate replicated full vocabulary embedding state per rank.
14. Implement device-resident routing records for every active input/target token: global token ID, flattened batch position, active-mask state, destination owner rank, local owner-row index. Determine owner rank and local row index on GPU.
15. Compact records by destination rank on GPU; preserve exact original flattened positions; transfer through real NCCL communication; look up vectors only on owner rank local shard; return vectors and original-position metadata; scatter into original batch layout on GPU; preserve padding and invalid-token rejection.
16. Implement real NCCL grouped peer communication or real NCCL-compatible all-to-all; never emulate through host buffers; never route through CPU memory; never serialize through JSON/files/temporary arrays.
17. Use bounded GPU buffers with validated capacities; compute and validate routing counts, sender offsets, receiver offsets, destination/source ranks, token bounds, local-row bounds, active masks, message shapes; fail explicitly on invalid distributed routing state.
18. Implement GPU-resident sparse embedding-gradient routing: gather only active token-position gradients on GPU; attach original global token ID, owner rank, local owner-row index, source-position metadata; compact by owner rank; transfer with real NCCL; on owning GPU group equal local token IDs and perform device-side segmented reduction; apply optimizer updates only to owned local source embedding rows; preserve gradient clipping and normalized-gradient behavior; never materialize dense [vocab_size][model_dim] gradient; never call dense full-vocabulary embedding-gradient all-reduce; never gather full gradients to rank zero; never use CPU accumulation for repeated tokens.
19. Create frozen target shards as GPU-to-GPU snapshot of matching local source shard; never initialize through GPU-to-CPU-to-GPU; never allocate gradient buffers or optimizer state for frozen targets; prevent source updates from mutating frozen targets.
20. Scale packed model-state tensors on GPU; expose packed raw device buffers safely; enqueue NCCL collectives through grouped operations for packed weights_s, weights_t, velocity_s, velocity_t; never issue one all-reduce per layer and per matrix.
21. Do not synchronize CUDA unnecessarily; synchronize only when real dependency requires visibility between Futhark work, NCCL work, scalar result retrieval, checkpoint I/O, or externally observable completion.
22. Replace distributed embedding checkpoint persistence with native rank-sharded checkpoint files; do not gather full embedding tables during normal checkpoint saving.
23. Write validated checkpoint manifest with format version, world size, rank count, rank-to-vocabulary-range mapping, vocabulary size, tokenizer identity and serialized state, model dimension, number of layers, packed RSF/OFTB state metadata, optimizer configuration, frozen-target configuration, shard file names, shard byte sizes, integrity hashes, exact tensor shapes, checkpoint compatibility metadata.
24. Write each rank local source embedding shard, local optimizer state, local frozen target shard directly from owned state; store replicated packed RSF/OFTB state without ambiguity; never require full embedding gathers for loading.
25. Loading requires identical world size, rank mapping, vocabulary partition, tokenizer state, model shape, shard layout; reject missing shards, duplicate shards, malformed manifests, invalid offsets/lengths/hashes, rank mismatch, world-size mismatch, tokenizer mismatch, model mismatch, checkpoint-version mismatch, trailing corrupted data; never silently fall back to legacy replicated embedding storage.
26. Implement explicit offline inference export as separate operation that gathers shards only for export; never invoke export gathering during ordinary training.
27. Update inference loading, model export, checkpoint metadata parsing, tokenizer binding, and all relevant callers so inference artifacts remain complete and valid; preserve single-rank inference and explicit exported artifacts.
28. Remove obsolete CPU one-hot training paths, per-layer host training dispatch, stale duplicate accelerator state, dense embedding-gradient all-reduce, replicated distributed embedding state, stale Futhark declarations, GPU-to-CPU-to-GPU frozen-target initialization, and invalid fallback branches; leave no dead code, dormant partial paths, or feature flags selecting incomplete behavior.
29. Build distributed CUDA target from clean workspace using real Futhark CUDA generator; run all existing relevant tests.
30. Add deterministic tests for: fused multi-layer RSF/OFTB forward/backward/update equivalence against mathematical reference within explicit f16/f32 tolerances; packed-state initialization/update/extraction/replacement/save/load/destruction; non-divisible vocabulary shard layouts; local and remote token routing; sparse repeated-token gradient reduction; sparse multi-rank embedding updates compared with deterministic small reference; frozen target shard immutability; grouped packed model-state reduction; native sharded checkpoint round trips; invalid checkpoint rejection; world-size mismatch rejection; rank-layout mismatch rejection; no dense full-vocabulary gradient allocation in multi-rank training; no CPU one-hot fallback in distributed training; no host-side per-layer model training dispatch loop.
31. Benchmark old and upgraded implementations with identical model dimensions, layer count, vocabulary size, sequence length, batch size, GPU count, dataset partition, and training steps; require evidence of reduced host dispatch count, reduced distributed embedding memory per rank, elimination of dense embedding-gradient all-reduce, and correct finite losses.
32. Write complete unabridged production-ready code for every changed file without truncation, ellipses, omissions, or placeholders.
33. Set epochs=1, max_tokens=10_000_000_000, single GPU world_size=1, GPU spec B200:1, model_dim=11200, num_layers=12, local_batch_size=2.
34. Commit to branch arena/019fe01d-drop, push, open pull request with title describing fused GPU-resident RSF/OFTB and body listing model parameters, epoch/token limits, GPU count, and eliminated behaviors.
